# 推甄戰備室 — 專案交接文件

> 供下一個 AI 對話框快速了解專案現況，無需重新詢問使用者。
>
> **先讀這三段就能開始工作**：① 下方「專案概述」② 「★ 內容模型」的五條撰寫規則
> ③ 「★ 待辦」的 P0。若使用者說「繼續補內容」，直接照 P0 的順序做，程式碼不用動。

---

## 專案概述

**名稱**：推甄戰備室
**目標**：為研究所推甄準備管院五科。兩個需求，順序有意義——(1) 面試被問到專業知識時答得上來，(2) 銜接研究所課程。教材因此一律做成雙層（面試層／進階層）。
**起造日**：2026-08-09
**型態**：純前端 PWA，無後端、無帳號、無外部依賴
**資料**：全部存 localStorage（目標日期等設定皆可在 App 內調整）

---

## 本機路徑與指令

```
C:\Users\user\Downloads\生成式AI人文導論\我自己想要開發的專案\管理學相關題庫\
```

```bash
python serve.py     # 本機預覽，port 8770（.claude/launch.json 已設定）
```

**已部署**：GitHub Pages，`main` 分支根目錄。沒有建置步驟，`git push` 後約一兩分鐘自動更新。

**推 GitHub 前一律先問過使用者**——這是對外發布的動作，不要自己推。

---

## 檔案結構

```
管理學相關題庫/
├── index.html          # 單頁四分頁 + SVG 圖示庫（<symbol>）
├── manifest.json       # PWA 設定
├── sw.js               # Service Worker（Stale-While-Revalidate）
├── serve.py            # 開發伺服器（強制 no-cache）
├── README.md           # 給使用者看的使用說明 + 67 天讀書排程
├── .claude/launch.json # port 8770
├── css/
│   ├── tokens.css      # 色票與版面變數、五科配色、深色模式
│   └── style.css       # 版面與元件
├── js/
│   ├── app.js          # ★ 進入點：分頁路由、四個畫面渲染、設定、匯出匯入
│   ├── storage.js      # ★ localStorage 持久層、sanitize、匯出匯入
│   ├── srs.js          # ★ 間隔複習排程（SM-2 改良版）
│   ├── content.js      # 載入教材並攤平成四份索引
│   ├── plan.js         # 倒數、每日目標、進度、統計聚合
│   ├── ui.js           # DOM 工具、輕量 Markdown、Sheet／Modal／Toast
│   ├── flash.js        # 閃卡引擎
│   ├── quiz.js         # 選擇題引擎 + 錯題本
│   ├── essay.js        # 申論自評
│   └── interview.js    # 面試模擬 + 我的素材庫 + 各校側重
├── content/
│   ├── index.js        # 科目註冊表 + 通用面試題 + 各校側重
│   ├── marketing.js    # 行銷管理（第 1–10 章，已完成）
│   ├── management.js   # 管理學（第 1–10 章，已完成）
│   ├── economics.js    # 經濟學（第 1–10 章，已完成）
│   ├── statistics.js   # 統計學（第 1–10 章，已完成）
│   └── finance.js      # 財務管理（第 1–3 章，續補中）
├── tools/              # 內容作業工具（純 Node，無外部套件）
│   ├── export-md.mjs        # 匯出教材題庫成 Markdown（給第三方檢查）+ 品質檢核
│   ├── splice-chapter.mjs   # 把新章節插進科目檔並更新 chapters 陣列
│   ├── append-concept.mjs   # 把新知識點附加到既有章節的「尾端」（強制 append-only）
│   └── escape-ticks.mjs     # 把行內公式的反引號 escape 成 \`（漏寫會造成語法錯誤）
└── icons/              # 192/512/maskable-512 PNG（Pillow 產生）
```

**沒有 stats.js**——原本規劃有，實作時發現統計聚合只有三個函式，併進 `plan.js`（`recentDays`／`accuracy`／`countdownText`）比多開一個近乎空的模組乾淨。

---

## 技術架構

| 項目 | 選擇 | 理由 |
|------|------|------|
| 框架 | 無，Vanilla JS + ES Modules | 沒有 build step 就沒有 node_modules，推上去即部署 |
| CSS | 原生 CSS + 變數 | 沿用既有專案規矩，且不想為離線 PWA 增加外部依賴 |
| Markdown | 自己寫 30 行（`ui.js` 的 `md()`） | 教材只用到 `**粗體**`、清單、段落三種語法，為此引入函式庫不划算 |
| 數學公式 | HTML + CSS 排版 | 統計／財管之後要用，但**不引入 KaTeX**——外部依賴會破壞離線 |
| 圖示 | 全 SVG `<symbol>`，不用 emoji | emoji 跨平台長相不一致，且無法吃 CSS 變數配色 |
| 儲存 | localStorage | 零成本零設定，無隱私疑慮 |

**外部依賴數量：0。**

---

## ★ 內容模型（動內容前務必先讀）

**核心決策：以「知識點」為中心，而非以「題目」為中心。**

一個知識點物件同時餵養四種練習模式。寫一個知識點約 15 分鐘，一次產出 2–4 張閃卡 + 3 題選擇 + 1 題申論 + 1 題口試。若四種模式各自維護資料，同樣的產出要花四倍時間——這是 67 天內兩科寫得完的唯一辦法。

```js
{
  id: 'mkt-05-01',              // 章節 id + 兩位序號
  title: '…',
  tags: ['必考', '核心'],        // '必考' 會被「只練必考」篩選到
  source: 'Kotler & Keller…',   // 出處學者，方便使用者對照課本抽查
  brief: '…',                   // 面試層：30 秒能講出口
  advanced: '…',                // 進階層：模型細節、限制、學術爭論
  example: '…',                 // 台灣實務案例
  terms:  [{ term, en, def, tip }],              // → 閃卡（tip 是「面試講法」）
  mcqs:   [{ q, options[4], answer, explain }],  // → 選擇題
  essays: [{ q, outline[], rubric[], sample }],  // → 申論自評
  oral:   [{ q, framework[], pitfall, schools }] // → 面試模擬
}
```

### 撰寫規則（違反會弄壞使用者的進度）

1. **練習項目的 id 由「知識點 id + 陣列位置」推導**（`c:<id>:<i>`、`q:<id>:<i>`…）。
   所以 `terms` / `mcqs` / `essays` / `oral` **一律往陣列尾端加**，不要插在中間，
   否則使用者已累積的 SRS 排程會整批錯位對到別題。
2. **章節與知識點 id 一經發布就不要改。**
3. **選擇題選項每次出題都會洗牌**，所以**詳解裡不要寫「選項 (B) 錯在…」**。
4. `brief` / `advanced` / `example` 支援的語法：`**粗體**`、`- ` 清單、`1. ` 編號清單、空行分段、**Markdown 表格**、**`[[知識點 id]]` 跨章連結**。渲染前會先跳脫 HTML，內容打錯不會弄壞版面。
   **跨章連結**（2026-08-10 補上渲染）：`[[mgt-08-02]]` 會被 `ui.js` 的 `xref()` 換成可點擊的知識點標題，點了直接跳過去。
   找不到對應 id 時**保留原文** `[[xxx]]`，所以打錯 id 會在畫面上直接看得出來。
   這一項與表格是同一類歷史錯誤：**寫作規範先訂了、渲染沒跟上**，內容裡的連結曾長期以原文外洩。
   **行內公式**（2026-08-10 補上支援）：`` `Ed = (ΔQ/Q) ÷ (ΔP/P)` `` 會被渲染成等寬字體加淡底，讓算式從中文內文裡跳出來。
   ★ **內容欄位本身是樣板字串，公式的反引號必須寫成 `` \` `` escape**，否則會截斷字串造成語法錯誤。
   分數、上下標、根號目前只能用文字近似（`ΔQ/Q`、`Q^d`）。
   **表格**（2026-08-10 補上支援）：標題列 → 分隔列 → 內容列，例如
   ```
   | 版本 | 導向 |
   |---|---|
   | 1.0 | 產品導向 |
   ```
   `md()` 會包一層 `.tablewrap`（可橫向捲動），窄螢幕與大字級都不會把版面撐寬。
   第一批教材寫了 10 處表格但當時 `md()` 不支援，分隔列 `|---|---|` 直接以文字外洩——
   若日後再擴充語法，記得**先確認 `md()` 支援再寫進內容**。
5. **章節 id 已按最終十章預留**（`mkt-01`…`mkt-10`、`mgt-01`…`mgt-10`），且章節物件帶顯式的 `no` 欄位。因此現在只有第 1、5、6 章時，畫面上仍正確顯示「第 5 章」而非「第 2 章」，之後補中間章節也不影響現有進度。

---

## ★ SRS 排程（`srs.js`，改動前務必先讀）

**不是標準 SM-2。** 原版設計給「終身保留」，熟題間隔會拉到好幾個月——但這個 App 服務的是一個有明確截止日的衝刺，排到考後才複習等於沒複習。兩項關鍵調整：

### 1. 間隔上限 = `min(21, 距離目標日的天數)`

保證每個項目在面試前至少再滾一輪。已驗證：目標日剩 3 天時，`intervalCap()` 回傳 3，連評四次「秒答」間隔仍停在 3。

### 2. 考前 `sprintDays`（預設 14）天自動進衝刺

`buildQueue()` 改為忽略到期日，用權重排序全刷：**錯題 +4 / 標記 +2 / 必考 +1**，同權重時 `staleness`（lapses 多、ease 低）優先。介面同步轉紅。

### 評分與間隔

四級：`0 忘記 / 1 勉強 / 2 會 / 3 秒答`

```
grade 0 → reps=0, lapses++, ease-0.2, interval=0（今天再來，且排回本輪隊尾）
grade≥1 → reps++, ease+[-0.15, 0, +0.15]
          reps 0→[1,1,2]  reps 1→[2,3,5]  reps≥2→max(prev+1, round(prev×factor))
          factor: 勉強 1.2 / 會 ease / 秒答 ease×1.25
          ease 夾在 [1.3, 2.8]，interval 夾在 intervalCap()
```

已驗證的成長序列（一路評「會」）：`1 → 3 → 8 → 20 → 21 → 21 …`

### ★ `newQuota()` 是每日目標與出題佇列的**單一來源**

```js
newQuota(remainingNew) = min(remainingNew, max(settings.dailyNewTarget, ceil(remainingNew / 剩餘天數)))
```

`plan.js` 算每日目標、`buildQueue()` 發新題，兩邊都呼叫它。**不要讓它們各自算**——第一版就是因為目標用「平攤值」而佇列用另一個 cap，出現「目標 10 題但佇列只給 6 題」這種永遠達不成的情況。

`dailyNewTarget` 是**基準不是上限**：題庫小、天數多時由它撐住（否則會算出「每天 1 張卡」這種不值得打開 App 的目標）；進度落後時平攤值會超過它，該追的量誠實呈現。

---

## 資料 Schema

localStorage key：`gradprep.v1`

```js
{
  schemaVersion: 1,
  settings: { targetDate, sprintDays, dailyNewTarget,
              mcqPerSession, cardPerSession, oralSeconds,
              textSize },                                  // html font-size（px），12–30
  srs:     { 'c:<conceptId>:<i>' | 'q:<conceptId>:<i>':
             { ease, interval, due, reps, lapses, last } },
  wrong:   { '<mcqId>': { streak, count, lastAt } },   // 連續答對 2 次才刪除
  starred: ['<conceptId>'],
  read:    { '<conceptId>': 'YYYY-MM-DD' },
  essayLog:[{ essayId, score, at, answer }],           // 保留最近 500 筆
  oralLog: [{ oralId, seconds, at }],                  // 保留最近 500 筆
  stories: [{ id, title, situation, action, result, theory }],
  daily:   { 'YYYY-MM-DD': { cards, mcqs, mcqRight, essays, oral, newConcepts } },
  streak:  { current, best, lastDay }
}
```

**`sanitize()` 是防禦式的**：一律夾範圍、退回預設值。已驗證塞入 `ease: 99`、`interval: 'abc'`、`starred: 'not-an-array'`、負數 streak 等爛值後 App 不會掛。

**日期一律用本地時區**（`storage.dateKey()`）。**不要改用 `toISOString()`**，那是 UTC，跨半夜會算錯天。

**匯入驗證刻意嚴格**：拒絕陣列、拒絕不含任何已知 key 的物件。第一版只檢查 `typeof === 'object'`，結果 JSON 陣列也會通過、sanitize 後變空白資料——使用者選錯檔案就會在看到「已匯入」的同時失去所有進度。這個 bug 已修，測試時請連帶回歸。

---

## 設計系統

```css
--indigo: #2E4374   /* 骨幹 */    --amber: #E08A3C   /* 強調 */
--paper:  #F7F5F0   /* 紙感底 */  --ink:   #1F2430
```

### ★ 可調字級（2026-08-09 加入）

使用者要求「把電腦拿遠一點也能看清楚」。作法是 **JS 把 `settings.textSize` 寫進 `html` 的 `font-size`**，全站字體一律用 `rem`，一個變數整頁縮放。五段：14 / 16 / 18 / 21 / 24 px（`storage.js` 的 `TEXT_SIZES`）。

三個實作要點，改版時不要弄丟：

1. **`--topbar-h` / `--tabbar-h` / `--maxw` 必須是 rem**，不能是 px。固定 px 在 24px 字級下會把文字切掉；`--maxw` 用 rem 還有個好處是桌機放大字時內容欄跟著加寬（45rem：16px→720px、24px→1080px），不會變成大字擠窄欄。
2. **padding / 圓角維持 px**，放大的是「字」不是整個版面被吹脹。
3. **`.tab .ic` 用 em 不用 px**，否則會變成大字配小圖示。`.ic` 本身已是 `1.25em`。

**`index.html` 的 `<head>` 有一小段同步腳本**先從 localStorage 讀 `textSize` 套上去。`js/app.js` 是 module（延後執行），只靠它會讓每次開啟先閃一下 16px 再跳到 24px——字調越大越刺眼。`app.js` 啟動時仍會再呼叫一次 `applyTextSize()`，讓 `sanitize()` 夾過範圍的值成為最終依據。

字級面板刻意做成**按下去立刻生效並存檔**，沒有確定鍵——使用者是把螢幕推遠了才在調，中間隔一個確認會逼他來回試。

**已驗證**：五段字級 × {手機 375、桌機 1280} × 四個分頁 + 知識點內文 + 閃卡評分列，皆無水平溢出與文字截斷。順帶修掉一個相關問題：作答層標題原本是「科目・知識點名稱」，在大字級會被 ellipsis 截掉，已改成只放科目名（知識點名稱在內文本來就是 h2）。

五科各有主色，由 `body[data-subject]` 切換 `--accent`：
行銷 `#D96A3C`／管理 `#3E7C6A`／經濟 `#5B6BB5`／統計 `#8A5FA8`／財管 `#B8862B`。
進入某一科的教材或練習時整頁換色，「我現在在讀哪一科」不必靠讀字判斷。

深色模式由 `prefers-color-scheme` 驅動，五科主色另有一組較亮的深色版本。

---

## 已驗證項目（2026-08-09／08-10）

SRS 間隔成長與 21 天封頂、目標日逼近時上限跟著縮、ease 上下限、忘記後 reps 歸零並排回隊尾、衝刺模式切換、每日目標與佇列數量一致、錯題本進出規則（錯→進，對 1 次留，對 2 次出）、選項洗牌、閃卡翻面與四級自評、申論勾選要點即時重算建議分數與作答存檔、口試牆鐘計時與關閉後清理、素材庫 CRUD 並在口試 review 列出、匯出匯入往返完全一致、爛 JSON 與錯誤格式被擋下且不動既有進度、手機 375×812 與桌機 1280×860 四個分頁皆無水平溢出、深色模式對比、**關掉伺服器後離線完整可用**（21 個資源全快取，仍能開教材與作答）。

08-10 追加：五段字級 ×{手機、桌機} 無溢出／截斷、字級在首次繪製前套用（無閃爍）、任務卡與工具列標題副標分行、四種作答層底部按鈕置中且左右對稱（誤差 < 2px）、手機滿版鍵與雙鍵列不受影響。

08-10 補完兩科十章後追加驗證：兩科各 10 章正確顯示且章號無跳號、`[[知識點]]` 跨章連結渲染為可點擊標題並能正確跳轉（mkt-02-01 → mkt-02-02 實測）、跨章連結吃科目主色（管理學顯示 `rgb(62,124,106)`）、內文無 `[[` 原文外洩、新章節表格正常渲染、選擇題連答 6 題（涵蓋 mgt-10-03／mkt-07-03／mgt-06-02 等新內容）皆正常出題與顯示詳解、詳解無選項代號外洩、手機 375 與 24px 大字級皆無水平溢出、console 無錯誤。

08-10 依第三方檢查報告補三個知識點後追加驗證：mkt-06 顯示 4 個知識點、mgt-04 顯示 5 個、新增的口試題正確落在 `o:mgt-04-01:1`（陣列尾端，既有進度不錯位）、新知識點的表格與跨科連結（mkt-06-04 連到 mgt-06-02／mgt-10-01）正常渲染、桌機與 24px 大字級皆無溢出、`e:mkt-01-01:0` 範答由 480 字補到 990 字後品質檢核「範答短於 500 字」歸零。

08-10 開經濟學後追加驗證：三科分頁正確顯示（35／34／31 點）、經濟學 10 章無跳號、主題色正確切換為 `#5B6BB5`（`body[data-subject="econ"]`）、**行內公式渲染為等寬字體並吃科目 accent-bg**（`rgb(234,236,247)`）、長算式 `overflow-wrap: anywhere` 在手機 375 不撐寬版面、**跨科連結可用**（eco-01-01 → mkt-07-01 等 5 條實測）、內文無反引號與 `[[` 原文外洩、選擇題連答 8 題涵蓋三科（含經濟的實物補貼、可信威脅題）皆正常、手機 375 與 24px 大字級皆無水平溢出。

08-10 第二次第三方報告（四項用詞／題幹拋光）後追加驗證：`mgt-04-04` 的 advanced／名詞卡／範答三處「綠郵」全數改為「綠色郵件」且無殘留、`e:mkt-05-04:0` 範答由 797 字增為 927 字、段落數維持 5 段且新增的 `**粗體**` 正確渲染、`q:mkt-03-01:2` 改為雙門檻題幹後選項仍 4 個、答案索引不變、詳解無選項代號、`og:1` 的 `schools` 渲染為 10 個段落無空段落、四處皆無 `[[` 與 `|---` 原文外洩、`node tools/export-md.mjs` 十一項檢核全為「無」。**四處皆為原地改字，未新增或插入陣列元素，既有 SRS 進度不受影響。**

08-10 統計學開檔（第 1–3 章）後追加驗證：四科分頁正確顯示（36／36／31／11 點）、統計學三章章號與知識點數正確（4／4／3）、主題色切換為 `#8A5FA8`（`body[data-subject="stat"]`）、**Unicode 上下標公式渲染為等寬字體並吃到統計專屬底色**（`rgb(241,234,246)`，即 `#F1EAF6`）、`x̄ = Σxᵢ / n`／`Ḡ = (x₁ × x₂ × … × xₙ)^(1/n)` 等含上下標的算式顯示正常、**計算題詳解渲染為 `<ol>` 編號清單＋`<code>` 行內公式**（實測 `q:stat-01-02:1` 幾何平均、`q:stat-02-02:0` 貝氏定理）、最長的統計詳解在 375 寬下無任何公式超出容器（`code` 的 `overflow-wrap: anywhere` 生效）、跨章連結 5 條渲染為可點擊標題、內文無反引號與 `[[` 原文外洩、手機 375 × 24px 大字級無水平溢出（最寬元素恰為 375px）、console 無錯誤。

08-10 統計學補完第 4–7 章後追加驗證：統計學七章章號連續無跳號、每章知識點數 4／4／3／3／4／3／4 共 25 點、75 題選擇題中 **19 題為含步驟的計算題**且全部渲染為 `<ol>`＋`<code>`、88 條跨章連結全部解析（`指向不存在知識點的連結` 為零）、25 個知識點的 `brief`／`advanced`／`example` 全欄位掃描**無 `[[` 原文、無反引號、無 `|---` 外洩**、手機 375 × 24px 大字級下**頁面本身無水平溢出**（唯一超過 375px 的元素為 441px 的表格，且確認包在 `.tablewrap` 內且 `overflow-x: auto`，屬預期設計）、第 4–7 章最長的三則計算題詳解在 375 寬下無任何公式超出容器、console 無錯誤。

08-10 統計學補完第 8–10 章（全科完成）後追加驗證：統計學十章章號連續、每章知識點數 4／4／3／3／4／3／4／4／3／3 共 35 點、105 題選擇題中 **22 題為含步驟的計算題**、**170 條跨章連結全部解析**（`指向不存在知識點的連結` 為零，且 `第 N 章` 純文字參照已全數改回連結或清除）、35 個知識點全欄位掃描無 `[[`／反引號／`|---` 外洩、手機 375 × 24px 大字級下**頁面與所有元素皆無超過 375px**（第 10 章無寬表格）、主題色 `#8A5FA8`、console 無錯誤。

**未能自動驗證**：真機加到主畫面後的行為、內容正確性（已標出處學者供使用者對照課本）、實際各校考古題偏好。

**已知潛在問題**（實測確認，見「★ 待辦」P3）：`wrongCount` 與 `readCount` 未過濾失效 id；`daily` 與 `srs` 永不清理。只要遵守「id 只能往陣列尾端加」就不會觸發。

---

## ★ 開發時的陷阱

**改了 js/ 或 content/ 卻沒生效** —— Service Worker 用 Stale-While-Revalidate，會先回舊快取。開發時每次改完要跑：

```js
(async () => {
  for (const r of await navigator.serviceWorker.getRegistrations()) await r.unregister();
  for (const k of await caches.keys()) await caches.delete(k);
  location.reload();
})()
```

`serve.py` 已送 `no-store`，但擋不住 SW。**改版時記得把 `sw.js` 的 `CACHE` 版本號加一**，並在新增 `js/` 或 `content/` 檔案時同步加進 `ASSETS`。

**用 preview 工具測試時**：
- `computer{action:'screenshot'}` 在這個環境會逾時，改用 `javascript_tool` 讀 DOM 驗證
- 答題後整塊 sheet body 會重繪，**舊的 DOM 參考會脫離**——要重新 query 才拿得到新狀態
- 用 `await import('./js/srs.js')` 可在頁面內取得模組實例，直接驗證演算法，比點 UI 快很多

### ★ `<span>` 預設 inline 造成的版面錯位（已咬過三次）

`ui.js` 的 `el()` 產生什麼標籤就是什麼標籤，**不會自動補 `display`**。而 app.js 大量用 `<span>` 當結構容器，`<span>` 預設是 inline，會造成兩類症狀：

1. **該分行卻擠在同一行** —— 容器是 `<span>` 且未設 `display`，裡面兩個子 `<span>`（標題／說明）就會並排。已修：`.task__main`、`.row__main`。**日後新增這種「標題＋副標」結構時，容器一定要加 `display:flex; flex-direction:column`。**
2. **`margin:auto` 置中失效** —— `margin:auto` 的置中效果**只對 block 級盒子生效，inline 級（含 `inline-flex`）一律算 0**。`.btn` 預設 `inline-flex`，導致 `.sheet__foot > *` 的置中對它無效、按鈕貼左。已修：`.btn--block` 改為 `display:flex`。

改版面後的快速自檢（在 console 跑）：

```js
// 找出「應該分行卻擠在一起」的容器
document.querySelectorAll('[class*="__main"]').forEach(m => {
  if (getComputedStyle(m).display.includes('inline'))
    console.warn('容器仍是 inline：', m.className);
});
```

---

## 開發慣例

- Windows 環境，git 指令走終端機
- 內容與註解一律繁體中文
- 修改後請依上方「已驗證項目」重跑一次回歸，特別是 SRS 數學與匯入驗證這兩處——它們都曾經有過會靜默出錯的 bug

---

## 現況盤點（2026-08-10 更新）

### 教材完成度

| 科目 | 章節進度 | 知識點 |
|---|---|---|
| 行銷管理 | **10 / 10 章　完成** | 36 |
| 管理學 | **10 / 10 章　完成** | 37 |
| 經濟學 | **10 / 10 章　完成** | 32 |
| 統計學 | **10 / 10 章　完成** | 35 |
| 財務管理 | **3 / 10 章　續補中** | 9 |

合計 **149 個知識點、589 張名詞卡、452 題選擇、149 題申論、159 題口試**（含 9 題通用面試題）、5 間學校側重。

**程式面功能已完整，瓶頸完全在內容。** 行銷、管理、經濟三科十章全數完成；**統計學已開檔並完成第 1–3 章，且原本卡住這一科的兩個前置問題都已解決**（見下方「★ 統計／財管的公式與計算題（已定案）」）。財管尚未開檔。

### 已完成的章節（不要重複做）

**行銷管理（10/10，2026-08-10 補完）**
- mkt-01 行銷概論與價值創造（4 個知識點）
- mkt-02 行銷環境與競爭分析（4）── PESTEL／五力／競爭者分析與競爭地位／SWOT-TOWS
- mkt-03 消費者行為（3）
- mkt-04 行銷研究（3）── 研究流程與設計／資料來源與蒐集／抽樣與信效度
- mkt-05 STP（4）
- mkt-06 產品與品牌權益（4）── 產品層次／PLC／品牌權益／**服務品質 PZB 缺口模型與 SERVQUAL**
- mkt-07 訂價（4）── 三大導向／行為訂價／新產品與組合訂價／價格差異化與價格戰
- mkt-08 通路與零售（3）── 通路功能與設計／密度 VMS 與衝突／零售業態與全通路
- mkt-09 整合行銷傳播 IMC（3）
- mkt-10 數位行銷與新興議題（4）

**管理學（10/10，2026-08-10 補完）**
- mgt-01 管理概論：功能、角色與技能（3）
- mgt-02 管理思想的演進（4）── 科學管理／Fayol 與 Weber／霍桑與行為學派／計量系統權變
- mgt-03 決策（3）── 決策程序與類型／有限理性與直覺／偏誤與群體決策
- mgt-04 規劃與策略（5）── SWOT-TOWS／五力／三大競爭策略／**Ansoff 與 BCG**／**核心競爭力、垂直整合與交易成本**
- mgt-05 組織結構與文化（4）
- mgt-06 人力資源管理（3）── 規劃招募甄選／訓練與績效評估／薪酬留才與 SHRM
- mgt-07 領導（4）
- mgt-08 激勵理論（3）
- mgt-09 溝通與團隊（3）── 溝通程序與障礙／方向與網絡／團隊發展與衝突
- mgt-10 控制、變革與創新（4）── 控制程序／BSC／Lewin 與 Kotter／創新與學習型組織

**經濟學（10/10，2026-08-10 建檔並補完）**
- eco-01 供給、需求與市場均衡（3）── 供需與價格機能／剩餘與市場效率／機會成本與比較利益
- eco-02 彈性（3）── 需求價格彈性／所得交叉供給彈性／稅負歸宿與農產品悖論
- eco-03 消費者選擇與效用（3）── 邊際效用／無異曲線與預算線／替代所得效果與行為修正
- eco-04 生產與成本（3）── 生產函數與邊際報酬／短期成本／長期成本與規模經濟
- eco-05 市場結構（4）── 完全競爭／獨占／獨占性競爭／寡占
- eco-06 賽局理論（3）── 納許均衡／囚犯困境與重複賽局／序列賽局與承諾
- eco-07 市場失靈與政府（3）── 外部性與 Coase／公共財與共有資源／資訊不對稱
- eco-08 總體經濟指標（3）── GDP／物價與通膨／失業
- eco-09 貨幣、利率與總體政策（3）── 貨幣與銀行／貨幣政策／AD-AS 與財政政策
- eco-10 國際貿易與匯率（3）── 貿易利得與政策／匯率決定／國際收支與開放經濟

### ★ 已知的跨科重複（不要當成 bug 去「修」）

**同一個模型在不同科目各有一份，這是刻意保留的**：

| 模型 | 行銷版 | 管理版 | 經濟版 |
|---|---|---|---|
| SWOT／TOWS | mkt-02-04 | mgt-04-01 | — |
| Porter 五力 | mkt-02-02 | mgt-04-02 | eco-05-04（寡占） |

**成因**：mgt-04 先寫，mkt-02 是後來補的，當時沒發現重複。**判斷後決定保留而非刪除**，理由有三：
1. **切入角度確實不同**——行銷版把結論推到 STP 與行銷組合，管理版推到策略選擇，經濟版談市場結構與賽局。
2. **使用者是分科複習的**，讀行銷時不會想跳到管理科找五力。
3. **刪除會破壞 SRS**——id 一經發布就不能動（見撰寫規則 2）。

**已在 mgt-04-01 與 mgt-04-02 的 `advanced` 末端加上「★ 跨科提醒」互相連結**，讓重複變成可導航的互補而不是混淆。**日後新增內容時若又遇到同一個模型，比照辦理：加跨科連結並說明各自的側重，不要刪。**

**另注意 mgt-04 目前有 5 個知識點**，超過「每章 3–4 個」的一般規格。這是因為策略是高頻考點且原本只有三個模型，補上 Ansoff／BCG 與核心競爭力／交易成本後成為 5 個。**這是有意識的例外，不是失控。**

---

## ★ 待辦（依重要性排序）

### P0 — 補教材（其他事都可以往後排）

程式碼**完全不用動**，只往 `content/` 加資料。

**行銷、管理、經濟、統計四科的十章已於 2026-08-10 全數補完。**

**下一步是財務管理整科**（`content/finance.js` 尚未建立，`tokens.css` 已預留 `--sub-finance: #B8862B`）。
建議章節：貨幣時間價值 → 風險與報酬 → 資本資產定價 → 債券評價 → 股票評價 → 資本預算 → 資金成本與資本結構 → 股利政策 → 營運資金管理 → 財務報表分析。

**★ 財管開工前唯一要先確認的是現值公式的排版。** 統計學已證實 Unicode 上下標＋單行分數足以應付（見下方「公式與計算題」），但財管的 `PV = FV / (1+r)ⁿ`、年金現值因子、WACC 這類式子巢狀較深，**照既定作法先試寫第 1 章再決定**，不要整科寫完才發現不可讀。

**分批寫一科的向前連結問題已全數清理**：統計學十章之間的 170 條 `[[ ]]` 連結全部指向存在的知識點，`第 N 章` 的純文字參照已無殘留。

### ★ 統計／財管的公式與計算題（已定案，不要再改回去）

這兩件事原本列為「開工前必須先解決」，2026-08-10 試寫統計第 1 章後定案：

1. **計算題不加新欄位，直接寫進 `explain`。**
   選擇題詳解是用 `ui.js` 的 `md()` 渲染的，本來就支援編號清單、粗體與行內公式。
   寫法：先列已知 → 一行一步 → 最後給答案，每步的算式用行內公式包起來。
   **實測渲染為 `<ol>` + `<code>`，零程式改動、不動 schema、不影響 SRS id 推導。**
   （原本考慮的 `steps[]` 欄位不需要，加了反而要改 `quiz.js`。）
2. **公式用 Unicode 直接寫，不引入 KaTeX。**
   `x̄ ȳ p̂ μ σ σ² s² Σ √ ≤ ≥ ≠ ± α β λ χ² H₀ H₁ x₁ x₂ xᵢ n₁ n₂` 都能正常顯示，
   比先前的文字近似（`Q^d`、`H_0`）好讀很多。**分數一律用 ` / ` 寫成單行**，例如
   `` `z = (x̄ − μ) / (σ/√n)` ``。已實測手機 375 寬 × 24px 大字級無溢出。

**新增科目**：寫 `content/<科目>.js`，在 `content/index.js` 匯入並放進 `SUBJECTS`；`tokens.css` 已預留 `--sub-econ` / `--sub-stat` / `--sub-finance` 三個主色。**記得把新檔案加進 `sw.js` 的 `ASSETS` 並把 `CACHE` 版本號加一。**（統計學這次已做，`CACHE` 現為 `gradprep-v12`。）

### ★ 分批寫一科時的兩個坑（統計學踩過，會靜默出錯）

1. **不要寫指向「還沒寫的章節」的跨章連結。**
   統計第 1 章寫了 `[[stat-07-02]]`、`[[stat-05-01]]` 等 6 條連結指向尚未存在的知識點，
   `xref()` 找不到 id 時**保留原文**，畫面上會直接出現 `[[stat-07-02]]`。
   匯出腳本的「指向不存在知識點的連結」會抓到，但**寫的當下不會有任何錯誤**。
   作法：**同批會寫到的章節才用 `[[ ]]`，跨到未來章節一律寫成純文字**（「第 7 章的迴歸」），
   等那一章寫了再回頭加連結。
2. **單引號字串裡的公式與撇號要用對的跳脫。**
   內容欄位有兩種寫法，跳脫規則不同，混用會造成語法錯誤：
   - **樣板字串**（`` ` `` 包住的 `brief`／`advanced`／`example`）：公式的反引號寫 `` \` ``
   - **單引號字串**（名詞卡 `def`、`tip`、詳解 `explain` 等）：反引號**不必**跳脫，
     但字串內的撇號要寫 `\'`。統計第 2 章曾把 `P(A')` 寫成 `P(A\\')`，
     `\\` 是跳脫的反斜線、接著的 `'` 直接把字串結束掉 → `SyntaxError`。
     **含撇號時最省事的作法是整個欄位改用雙引號**（`"Bayes' Theorem"`）。

**動內容前務必先讀上方「內容模型」的五條撰寫規則**，尤其 id 只能往陣列尾端加。

### 每章的產出規格（照這個做就會跟既有內容一致）

- 每章 **3–4 個知識點**，每個知識點包含：
  - `brief` 面試層（30 秒講得出口，可用表格做比較）
  - `advanced` 進階層（模型細節、限制、學術爭論、跨章連結）
  - `example` 台灣或可驗證的實務案例（**避免對真實企業內部運作做無法查證的斷言**，用情境式舉例較安全）
  - `terms` 3–4 張名詞卡，每張含 `en` 英文名與 `tip` 面試講法
  - `mcqs` 3 題選擇，每題 4 選項 + 詳解（**詳解不可提及選項代號**）
  - `essays` 1 題申論，含 `outline` 答題大綱、`rubric` 評分要點、`sample` 完整範答（約 800–1200 字）
  - `oral` 1 題口試，含 `framework` 答題框架與 `pitfall` 常見地雷
- **跨章連結用 `[[概念 id]]`**（例如 `[[mgt-08-02]]`），這是拉開申論分數最有效的手法
- 寫完務必跑下方的品質檢核腳本

### ★ 內容品質檢核（每次補完內容都要跑）

**最快的方式是跑匯出腳本**——它不需要起站、不需要瀏覽器，跑完會直接印出檢核結果與最新統計數字：

```bash
node tools/export-md.mjs
```

**檢核共十一項，掃描範圍涵蓋知識點的 `brief`／`advanced`／`example`、選擇題 `explain` 與申論 `sample`。**（後兩者是 2026-08-10 補上的——統計學開始把公式與跨章連結寫進詳解與範答，原本只掃三個欄位等於沒檢查到。）

它同時會把教材與題庫輸出到 `export/`（總覽 1 檔 + **教材每科 1 檔**，檔名 `01a_`／`01b_`／`01c_`… 由 `SUBJECTS` 動態產生 + 題庫 3 檔，目前共 7 檔；可直接丟給 NotebookLM 之類的工具做第三方內容檢查；`export/` 已在 `.gitignore`）。**教材檔名編號刻意用 `01a/01b/01c` 而不是 `01/02/03`**——`03`–`05` 已被三個題庫檔佔用，而使用者的 NotebookLM 筆記本是照檔名建立來源的，重編號等於要他整本重傳；往後補統計、財管只會長成 `01d`／`01e`。腳本收尾會清掉上次留下、這次不再產生的舊編號檔（只認 `NN[a-z]_*.md` 這個命名規則，使用者自己放進 `export/` 的檔不會被刪）。檢核項目：選擇題格式、詳解是否提到選項代號、知識點欄位完整性、id 重複、表格語法、**跨章連結是否指向不存在的知識點**、名詞卡英文、知識點出處、範答長度。**全部應為「無」。**

以下是等價的瀏覽器 console 版本（需先 `python serve.py` 起站），改動 `ui.js` 的渲染時用它比較能反映真實畫面：

```js
(async () => {
  const c = await import('./js/content.js');
  const ui = await import('./js/ui.js');
  const bad = c.allMcqs.filter(m => !Array.isArray(m.options) || m.options.length !== 4
    || !Number.isInteger(m.answer) || m.answer < 0 || m.answer > 3 || !m.q || !m.explain).map(m => m.id);
  const letters = c.allMcqs.filter(m => /選項\s*[（(]?\s*[A-D]\s*[)）]?/.test(m.explain)).map(m => m.id);
  const incomplete = c.allConcepts.filter(x => !x.brief || !x.terms?.length
    || !x.mcqs?.length || !x.essays?.length || !x.oral?.length).map(x => x.id);
  const ids = c.allConcepts.map(x => x.id);
  const dupIds = [...new Set(ids.filter((v,i) => ids.indexOf(v) !== i))];
  const itemIds = [...c.allCards, ...c.allMcqs, ...c.allEssays, ...c.allOrals].map(x => x.id);
  const dupItemIds = [...new Set(itemIds.filter((v,i) => itemIds.indexOf(v) !== i))];
  const tables = [];
  for (const x of c.allConcepts) for (const f of ['brief','advanced','example']) {
    const v = x[f];
    if (typeof v === 'string' && /^\s*\|.*\|\s*$/m.test(v)) {
      const h = ui.md(v);
      if (!h.includes('<table>') || h.includes('|---')) tables.push(`${x.id}.${f}`);
    }
  }
  return { bad, letters, incomplete, dupIds, dupItemIds, brokenTables: tables,
    totals: { concepts: c.allConcepts.length, cards: c.allCards.length,
      mcqs: c.allMcqs.length, essays: c.allEssays.length, orals: c.allOrals.length } };
})()
```

全部陣列都應該是空的。`totals` 拿來更新這份文件與 README 的數字。

### ★ 收到第三方（NotebookLM 等）檢查報告時的處理原則

使用者會把 `export/` 丟給 NotebookLM 做內容檢查，並把報告貼回來要求修正。**2026-08-10 第一次實作後得到的經驗：報告不能照單全收，必須逐項回到 `content/` 驗證。** 四種常見的失真：

1. **報告基於舊的匯出檔**。使用者不一定會重跑 `node tools/export-md.mjs`，所以「缺第 7、8 章」這類結論可能早就被補掉了。**先比對報告裡的統計數字與現況**，對不上就代表整份報告的範圍是舊的。
2. **報告混入使用者的其他來源**。同一個 NotebookLM 筆記本裡通常還有 YouTube 逐字稿、課堂 PDF。第一次的報告要求把「新宇／新運／新於航空」改成「星宇航空」、「玉安電腦」改成「王安電腦」——但這些字串在 `content/` 裡**出現 0 次**，錯誤來自使用者的語音轉文字講義，不是這個題庫。**動手前先 grep 確認字串真的存在。**
3. **把排版慣例誤判為錯誤**。報告指 `q:mkt-10-01:2` 的選項有「多餘半形空格」，實際上那是全站一致的**中英／中數之間加空格**的排版慣例（`5.0 時代`、`執行 STP`），而報告所描述的「全形逗號後有空格」根本不存在。**照改反而會製造不一致。**
4. **建議的補充可能與「避免無法查證的斷言」衝突**。報告曾要求在範答中補上某企業面對數位衝擊的具體轉型作為——這違反了產出規格中「避免對真實企業內部運作做無法查證的斷言」。**做法是保留該案例，但改寫成可觀察的事實與通用的策略邏輯。**

**真正該修的通常是這三類**：範答長度不足、確實缺漏的高頻考點、以及跨科重複或矛盾。第一次報告中被採納的即為此類（見下方 08-10 的變更紀錄）。

**回覆使用者時要誠實分類**：哪些採納、哪些不成立、理由是什麼。直接照單全收會把不存在的錯誤「修」進內容裡。

#### 第二次報告的處理結果（2026-08-10，已完成）

報告提出四大「核心知識缺口」，逐項 grep `content/` 驗證後的結論如下。
**✅ 標記的五項已於同日補完**（見下方「已採納並完成」）；**❌ 兩項不成立者未動**。

**❌ 不成立（內容已存在，照做會製造重複）**
- **「完全遺漏 Follett 的整合性衝突解決」** —— `management.js` 的 mgt-02-03 已有 Follett 的 integration 與情境法則；mgt-09-03 已有 Thomas 五種衝突處理，其中「統合（雙贏）」即整合取向。**真正缺的只是兩者之間沒有互相連結**，補一條 `[[ ]]` 即可，不必新增知識點。
- **「選擇題庫對古典／操作制約完全空白」** —— `marketing.js` 的 mkt-03-02 `advanced` 已有古典制約（Pavlov）、操作制約（Skinner）與認知學習，且有一張名詞卡。**確實沒有對應的選擇題**，但主題並非空白，補題即可。

**✅ 確實缺、且值得補**
- **Barnard 的 Zone of Indifference（無關區）** —— 全庫 0 命中。mgt-02-03 有權威接受論但沒有無關區，屬高鑑別度考點。
- **刺激類化（Stimulus Generalization）** —— 0 命中，可接在 mkt-03-02 的學習理論後面。
- **認知學習三型態（觀察／背誦／推論）** —— 現況只寫「透過觀察與模仿」，缺三分類。
- **HHI 指數** —— 0 命中。`economics.js` 的 eco-04-03 談產業集中度與最小效率規模，但沒有 HHI 的定義與計算，且這是少數可出計算題的產業結構考點。
- **M&A 的三大目標分類（策略／效率／財務）** —— `management.js` 有 12 處提到購併但無系統性分類；mgt-04-05 有垂直整合與交易成本可接。

**⚠ 補之前要注意**
- **基價點定價法與蓋瑞晚餐** —— 史實無誤（US Steel 的 Gary Dinner、Pittsburgh Plus 制度），但**這是美國反托拉斯史的細節，台灣推甄考出的機率低**，CP 值明顯低於上面五項。要補的話放 eco-05-04 寡占的 `advanced` 當延伸例即可，不必獨立成知識點。
- **唐吉訶德 CVDA、統一併購家樂福、振興券** —— 都是**案例補充而非理論缺口**（全通路本身 mkt-08-03 已寫得完整）。可作為既有知識點 `example` 的替換或補充，但要遵守「避免對企業內部運作做無法查證的斷言」，寫成可觀察的事實與通用邏輯。
- **「V-P-C 價值創造模型陷阱」** —— 報告只在最後的指令區塊出現這個詞，**前文四大缺口的說明裡從未解釋它是什麼**。不要憑字面猜測就寫進教材，**動工前先問使用者這是指什麼**。

**共通風險**：這份報告的來源是使用者 NotebookLM 裡的講義 PDF 與 YouTube 轉譯稿，**我們看不到原始出處**。可寫的只有通用學理表述，涉及特定講者的獨門分類或口述細節時，要標成一般文獻可查證的版本，並照規格在 `source` 標出處。

#### 已採納並完成（2026-08-10）

五項確認缺漏全數補完。**兩項以「原地擴寫既有知識點 + 陣列尾端附加」處理，三項新增知識點**：

| 項目 | 位置 | 作法 |
|---|---|---|
| Barnard 無關區 | `mgt-02-03` | `advanced` 原地擴寫；名詞卡加在 index 4、選擇題加在 index 3 |
| Follett 整合性衝突解決 | `mgt-02-03` + `mgt-09-03` | 兩處互加 `[[ ]]` 連結；名詞卡 index 5、選擇題 index 4 |
| 刺激類化／刺激辨別 | `mkt-03-03` | `advanced` 的學習理論段重寫為行為學派／認知學派兩大陣營；名詞卡 index 4、5 |
| 認知學習三型態 | `mkt-03-03` | 同上；名詞卡 index 6、選擇題 index 3–5（原本這裡**一題選擇題都沒有**） |
| HHI 指數 | **新增 `eco-05-05`** | 產業集中度的衡量（CRₙ 與 HHI），含併購增量 `2S₁S₂` 的推導與計算題 |
| M&A 三大目標分類 | **新增 `mgt-04-06`** | 購併與策略聯盟（策略／效率／財務三分類、綜效檢驗、贏家詛咒、整合模式） |

**★ 學習理論在 `mkt-03-03`（影響消費者行為的四大因素），不是 mkt-03-02。**
mkt-03-02 是「涉入度與四種購買行為」，容易找錯。

**★ 已實測確認既有 SRS id 完全未錯位**：`c:mgt-02-03:0–3` 與 `q:mgt-02-03:0–2`、`c:mkt-03-03:0–3` 與 `q:mkt-03-03:0–2` 的內容與改動前完全相同，新項目一律落在其後。

**`mgt-04` 現有 6 個知識點、`eco-05` 有 5 個**，皆超過「每章 3–4 個」的一般規格。與 mgt-04 原本的 5 個同理，**這是有意識的例外**：策略與市場結構都是高頻考點。

### P1 — 會實際影響讀書效率的功能缺口

- [ ] **錯題本瀏覽頁**（最有價值的一個）。目前只能「練錯題」，看不到**哪些知識點反覆答錯**——而這正是最強的診斷訊號，卻完全沒有呈現。應列出錯題並依 `wrong[id].count` 排序、可跳回該知識點。
- [ ] **鍵盤快捷鍵**。桌機刷卡全靠滑鼠點，速度差很多。建議：空白鍵翻面、`1`–`4` 對應四級評分、選擇題 `1`–`4` 選項、`Enter` 下一題、`Esc` 關閉（Esc 已實作）。
- [ ] **申論歷史回看**。`state.essayLog` 有存 `answer` 全文，但**沒有任何畫面顯示它**。同一題寫第二次時看不到上次寫了什麼，無從判斷有沒有進步。
- [ ] **到期分佈圖**。看不到未來幾天的複習負擔，可能某天突然湧入 80 張卡。「我」分頁加一張未來 14 天的長條圖即可。

### P2 — 教材品質的結構性缺口

- [ ] **沒有任何圖表**。BCG 矩陣、定位圖、PLC 曲線、需求供給圖本質上是視覺概念，目前全用文字描述，理解成本高。建議在 concept 加一個選用的 `diagram` 欄位放**內嵌 SVG**（維持零依賴，且能吃 CSS 變數配色，深色模式自動適應）。
- [x] ~~**沒有計算題**~~ ——已解決（2026-08-10）。統計學第 1–3 章的計算題把步驟寫在 `explain` 裡，靠 `md()` 渲染成編號清單＋行內公式，**不需要 `steps[]`**。詳見 P0 的「統計／財管的公式與計算題（已定案）」。
- [x] ~~**公式排版只解了一半**~~ ——已解決（2026-08-10）。改用 **Unicode 上下標**（`x̄`、`σ²`、`√`、`Σ`、`H₀`、`xᵢ`）取代原本的文字近似，配上既有的行內公式渲染已足夠可讀，**不引入 KaTeX**。分數一律寫成單行 ` / `。已實測手機 375 × 24px 無溢出。**財管的現值公式仍建議先試寫一章確認。**

### P3 — 穩健性（目前不會壞，但教材改版後會咬人）

以下三項已用程式實測確認，都屬**潛在**問題：只要遵守「id 只能往尾端加」就不會觸發，但值得補上防護。

- [ ] **錯題數字會灌水**。`plan.js` 的 `wrongCount` 用 `Object.keys(state.wrong).length`，**沒有過濾已不存在的題目 id**。實測：注入一筆失效 id 後，徽章顯示「錯題重練（2）」但實際只練得到 1 題。`quiz.js` 已有 `wrongIds()` 會過濾，但**它是死程式碼、沒有任何地方呼叫**——把 `plan.js` 改用它即可。
- [ ] **`readCount` 同樣未過濾**（`plan.js:52`）。若教材縮減，「已讀 N／總 M」的 N 可能大於 M。
- [ ] **`daily` 與 `srs` 永不清理**。實測 401 天的 `daily` 紀錄經過 `sanitize()` 後仍是 401 筆；失效的 `srs` 條目也會永久殘留。`essayLog`／`oralLog` 已有 500 筆上限，`daily` 可比照保留最近 400 天，`srs` 可在載入後比對 `content.js` 的索引清掉孤兒。

### P4 — 有想過但刻意沒做

- [ ] **Service Worker 更新提示**。目前是 SWR + `skipWaiting`，使用者會在**第二次開啟**才拿到新版，中間不會有任何提示。要做的話：偵測 `registration.waiting` 後跳一個「有新版本，點此更新」的 toast。
- [ ] **面試模擬錄音**（`MediaRecorder`，純本機不上傳）。能聽自己講話是很強的回饋，但會需要處理權限與儲存空間。
- [ ] **自訂學校**。`SCHOOLS` 目前寫死五間，使用者無法自己增刪。
- [ ] **雲端同步**。刻意不做——見下方「刻意不做的事」。

---

## 刻意不做的事（別好心加回來）

| 項目 | 為什麼不做 |
|---|---|
| 申論自動批改 | 學習訊號來自「自己先產出，再跟範答逐點對照」。機器給一個分數，人就會跳過對照那一步，而那一步才是真正記得住的原因。 |
| 任何外部 CDN／函式庫 | 零依賴是這個 App 能完全離線的前提。通勤時沒網路仍要能刷題，是它存在的主要情境之一。 |
| 雲端同步／帳號 | 一個人自己用，匯出匯入已足夠。加帳號要處理隱私、後端與成本，收益不成比例。 |
| emoji 當圖示 | 跨平台長相不一致，且無法吃 CSS 變數配色。全站用 SVG `<symbol>`。 |
| 標準 SM-2 的長間隔 | 服務的是有明確截止日的衝刺，排到考後才複習等於沒複習。見上方 SRS 章節。 |
